<!DOCTYPE html>
<html>
<head>
    <title>CFe{{ chave }}</title>
    <style>
        body {
            font-size: 7pt;
            font-family: Arial, Helvetica, sans-serif;
            margin: 0 auto;
           /* margin-left: -40px;*/
            margin-top: -10px;
            width: 300px; /* Valor da Largura */
        }

        div.conteudo {
            align-content: center;
        }


        .text-center {
            text-align: center;
        }

        .font-bold {
            font-weight: bold;
        }

        img.logo {
            width: 64px;
            height: 64px;
        }

        p.nome-emitente {
            font-size: 10pt;
            font-weight: bold;
        }

        .font-size-8 {
            font-size: 8pt;
        }

        hr {
            margin-top: -3px;
        }

    </style>
</head>
<body>


<table class="cabecalho">
    <tr>
        {% if logo %}
            <td>
                <img src="{{ logo }}" class="logo"/>
            </td>
        {% endif %}

        <td colspan="{{ (logo) ? 1 : 2 }}">
            <p class="nome-emitente">{{ emitente.xNome }}</p>
            <p class="">
                {{ emitente.enderEmit.xLgr }}, {{ emitente.enderEmit.nro }} - {{ emitente.enderEmit.xBairro }},
                {{ emitente.enderEmit.xMun }}
            </p>
            <p class="">
                CEP: {{ emitente.enderEmit.CEP }}
            </p>
        </td>
    </tr>
</table>
<table class="info-emitente">
    <tr>
        <td>
            CNPJ: {{ emitente.CNPJ }}
        </td>
        <td>
            IE: {{ emitente.IE }}
        </td>
        {% if emitente.IM is defined %}
            <td>IM: {{ emitente.IM }}</td>
        {% endif %}
    </tr>
</table>

<hr>

<p class="text-center font-bold font-size-8">
    Extrato No. {{ ide.nCFe }} <br/>
    CUPOM FISCAL ELETRÔNICO - SAT
</p>

<hr>

<p class="font-bold">
    # | COD | DESC | QTD | UN | VL.UNIT (R$) | ST | ALIQ | VL.ITEM (R$)
</p>

<hr>



<p class="text-center">
    {{ chave | barcode | raw }}
</p>

{% for prod in produtos %}
    <p>{{ prod.xProd }}</p>
{% endfor %}
</body>
</html>